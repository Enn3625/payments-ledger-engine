# Copy to backend/.env for local development.
DATABASE_URL=postgresql+psycopg://ledger:ledger@localhost:55432/ledger
TEST_DATABASE_URL=postgresql+psycopg://ledger:ledger@localhost:55432/ledger_test
ENVIRONMENT=local

# Shared secret the payment provider signs webhooks with. MUST be overridden
# with a real secret in any deployed environment.
WEBHOOK_SECRET=whsec_local_dev_secret
WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS=300

# Anomaly rules. Amounts are minor units (paise).
ANOMALY_AMOUNT_THRESHOLD=500000
ANOMALY_VELOCITY_MAX_CAPTURES=5
ANOMALY_VELOCITY_WINDOW_MINUTES=10

# JWT signing. MUST be overridden in any deployed environment: anyone holding
# this value can mint a valid admin token.
JWT_SECRET=jwt_local_dev_secret_change_me
ACCESS_TOKEN_EXPIRE_MINUTES=60
